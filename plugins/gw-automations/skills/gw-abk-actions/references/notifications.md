# Notifications

Actions for sending assignment notifications.

## Send Single Assignment Email

**Action class:** `SendSingleEmailNotificationAction`

Use this action after performing an assignment to send an email notification
to the person that received the assignment. The email is sent to
`assignment.assignedTo.Email`. If the assignment was to a Salesforce Queue,
`assignedTo` is null and the action does nothing.

The `body` is rendered as HTML. Both `subject` and `body` support the following
merge fields resolved against the assignment:

 * `{!item.name}` — the display name of the assigned record
 * `{!item.id}` — the Id of the assigned record
 * `{!item.detailUrl}` — the Salesforce URL of the assigned record
 * `{!assignedTo.Name}` — the full name of the assignee
 * `{!assignedTo.Email}` — the email address of the assignee

**Note:** We recommend specifying an org-wide email address via `orgWideEmailId`.
Without one, Salesforce uses the email of the user that invoked the Flow — if the
Flow is triggered by a system user (e.g. a platform event), sending will fail.

### Inputs

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `assignment` | `Assignment` | Yes | An Assignment generated by a previous action in the flow. |
| `body` | `String` | Yes | The HTML body of the email. Supports merge fields — see the action description. |
| `subject` | `String` | Yes | The subject of the email. Supports merge fields — see the action description. |
| `bcc` | `String` | No | A comma-delimited list of email addresses to BCC. |
| `orgWideEmailId` | `String` | No | The Id of an OrgWideEmailAddress record to use as the from address. |

### Outputs

| Field | Type | Description |
|-------|------|-------------|
| `success` | `Boolean` | True if the email send was sent successfully. |

---
